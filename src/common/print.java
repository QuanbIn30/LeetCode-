package common;

/**
 * @author MHLEVEL
 * @date 2020-09-12 11:10
 */
public class print {

    /*
    public static void printList (ListNode head) {
        StringBuilder str = new StringBuilder();
        str.append(head.val);
        while (head.next != null) {
            str.append(head.next.val);
            str.append("->");
            head = head.next;
        }
        System.out.println(str.toString());
    }
    */
}
